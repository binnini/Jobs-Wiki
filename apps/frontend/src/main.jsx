import React from "react";
import ReactDOM from "react-dom/client";
import JobsWikiPrototype from "./JobsWikiPrototype";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JobsWikiPrototype />
  </React.StrictMode>,
);
